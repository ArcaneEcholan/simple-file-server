let debugView = true;
// debugView = false;
const state = {
    debugView: debugView,
    logEnabled: true
};

const mutations = {};

const actions = {};

export default {
    namespaced: true,
    state,
    mutations,
    actions,
};
